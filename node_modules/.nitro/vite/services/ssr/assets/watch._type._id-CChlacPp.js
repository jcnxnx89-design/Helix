import { C as useProfile, S as updatePreferences, T as registerPlayer, _ as getProfile, b as saveProgress, c as cn, g as getPosition, n as Route, s as Button, w as publishNowPlaying } from "./router-ChHaMAyj.js";
import { n as EmptyState } from "./ui-states-CevxkTO-.js";
import { n as formatTime } from "./format-C-wS2g1W.js";
import { d as sourcesQuery, l as seasonQuery, r as movieQuery, u as showQuery } from "./queries-BPwnCAxF.js";
import * as React from "react";
import { useCallback, useEffect, useRef, useState } from "react";
import { useNavigate, useRouter } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { ArrowLeft, Loader2, Maximize, Minimize, Pause, Play, RotateCcw, RotateCw, SkipBack, SkipForward, Subtitles, Volume1, Volume2, VolumeX } from "lucide-react";
import * as SliderPrimitive from "@radix-ui/react-slider";
//#region src/components/ui/slider.tsx
var Slider = React.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsxs(SliderPrimitive.Root, {
	ref,
	className: cn("relative flex w-full touch-none select-none items-center", className),
	...props,
	children: [/* @__PURE__ */ jsx(SliderPrimitive.Track, {
		className: "relative h-1.5 w-full grow overflow-hidden rounded-full bg-primary/20",
		children: /* @__PURE__ */ jsx(SliderPrimitive.Range, { className: "absolute h-full bg-primary" })
	}), /* @__PURE__ */ jsx(SliderPrimitive.Thumb, { className: "block h-4 w-4 rounded-full border border-primary/50 bg-background shadow transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50" })]
}));
Slider.displayName = SliderPrimitive.Root.displayName;
//#endregion
//#region src/components/video/video-player.tsx
function VideoPlayer({ source, title, subtitle, poster, startAt = 0, onBack, onProgress, onEnded, onNext, onPrev }) {
	const shellRef = useRef(null);
	const videoRef = useRef(null);
	const hideTimer = useRef(null);
	const [ready, setReady] = useState(false);
	const [error, setError] = useState(null);
	const [paused, setPaused] = useState(true);
	const [position, setPosition] = useState(startAt);
	const [duration, setDuration] = useState(0);
	const [buffering, setBuffering] = useState(false);
	const [volume, setVolumeState] = useState(1);
	const [muted, setMuted] = useState(false);
	const [subsOn, setSubsOn] = useState(false);
	const [fullscreen, setFullscreen] = useState(false);
	const [controlsVisible, setControlsVisible] = useState(true);
	const showControls = useCallback(() => {
		setControlsVisible(true);
		if (hideTimer.current) window.clearTimeout(hideTimer.current);
		hideTimer.current = window.setTimeout(() => setControlsVisible(false), 3500);
	}, []);
	useEffect(() => {
		const video = videoRef.current;
		if (!video || source.kind === "iframe") return;
		setError(null);
		setReady(false);
		let destroy;
		const nativeHls = video.canPlayType("application/vnd.apple.mpegurl");
		if (source.kind === "hls" && !nativeHls) {
			let cancelled = false;
			import("hls.js").then(({ default: Hls }) => {
				if (cancelled) return;
				if (!Hls.isSupported()) {
					setError("This browser can't play HLS streams.");
					return;
				}
				const hls = new Hls({ enableWorker: true });
				hls.loadSource(source.url);
				hls.attachMedia(video);
				hls.on(Hls.Events.ERROR, (_e, data) => {
					if (data.fatal) setError("The stream could not be loaded.");
				});
				destroy = () => hls.destroy();
			});
			return () => {
				cancelled = true;
				destroy?.();
			};
		}
		video.src = source.url;
		return () => {
			video.removeAttribute("src");
			video.load();
		};
	}, [source]);
	useEffect(() => {
		const prefs = getProfile().preferences;
		const video = videoRef.current;
		if (!video) return;
		video.volume = prefs.volume;
		video.muted = prefs.muted;
		video.playbackRate = prefs.playbackRate;
		setVolumeState(prefs.volume);
		setMuted(prefs.muted);
		setSubsOn(prefs.subtitlesOn);
	}, []);
	const api = {
		playPause: () => {
			const v = videoRef.current;
			if (!v) return;
			if (v.paused) v.play();
			else v.pause();
			showControls();
		},
		seekBy: (s) => {
			const v = videoRef.current;
			if (v) v.currentTime = Math.max(0, Math.min(v.duration || 0, v.currentTime + s));
			showControls();
		},
		seekTo: (s) => {
			const v = videoRef.current;
			if (v) v.currentTime = Math.max(0, s);
			showControls();
		},
		adjustVolume: (d) => {
			const v = videoRef.current;
			if (!v) return;
			const next = Math.max(0, Math.min(1, v.volume + d));
			v.volume = next;
			v.muted = false;
			setVolumeState(next);
			setMuted(false);
			updatePreferences({
				volume: next,
				muted: false
			});
			showControls();
		},
		setVolume: (value) => {
			const v = videoRef.current;
			if (!v) return;
			const next = Math.max(0, Math.min(1, value));
			v.volume = next;
			setVolumeState(next);
			updatePreferences({ volume: next });
			showControls();
		},
		toggleMute: () => {
			const v = videoRef.current;
			if (!v) return;
			v.muted = !v.muted;
			setMuted(v.muted);
			updatePreferences({ muted: v.muted });
			showControls();
		},
		nextEpisode: () => onNext?.(),
		prevEpisode: () => onPrev?.(),
		toggleFullscreen: () => {
			const el = shellRef.current;
			if (!el) return;
			if (document.fullscreenElement) document.exitFullscreen();
			else el.requestFullscreen().catch(() => void 0);
		},
		toggleSubtitles: () => {
			setSubsOn((on) => {
				const next = !on;
				const v = videoRef.current;
				if (v) for (let i = 0; i < v.textTracks.length; i += 1) v.textTracks[i].mode = next && i === 0 ? "showing" : "hidden";
				updatePreferences({ subtitlesOn: next });
				return next;
			});
			showControls();
		}
	};
	const apiRef = useRef(api);
	apiRef.current = api;
	useEffect(() => {
		return registerPlayer({
			playPause: () => apiRef.current.playPause(),
			seekBy: (s) => apiRef.current.seekBy(s),
			seekTo: (s) => apiRef.current.seekTo(s),
			adjustVolume: (d) => apiRef.current.adjustVolume(d),
			setVolume: (v) => apiRef.current.setVolume(v),
			toggleMute: () => apiRef.current.toggleMute(),
			nextEpisode: () => apiRef.current.nextEpisode(),
			prevEpisode: () => apiRef.current.prevEpisode(),
			toggleFullscreen: () => apiRef.current.toggleFullscreen(),
			toggleSubtitles: () => apiRef.current.toggleSubtitles()
		});
	}, []);
	useEffect(() => {
		publishNowPlaying({
			title,
			subtitle: subtitle ?? null,
			poster: poster ?? null,
			position,
			duration,
			paused,
			volume,
			muted,
			subtitlesOn: subsOn
		});
	}, [
		title,
		subtitle,
		poster,
		position,
		duration,
		paused,
		volume,
		muted,
		subsOn
	]);
	useEffect(() => {
		const onKey = (e) => {
			const target = e.target;
			if (target && ["INPUT", "TEXTAREA"].includes(target.tagName)) return;
			switch (e.key) {
				case " ":
				case "k":
					e.preventDefault();
					apiRef.current.playPause();
					break;
				case "ArrowRight":
					apiRef.current.seekBy(10);
					break;
				case "ArrowLeft":
					apiRef.current.seekBy(-10);
					break;
				case "ArrowUp":
					apiRef.current.adjustVolume(.1);
					break;
				case "ArrowDown":
					apiRef.current.adjustVolume(-.1);
					break;
				case "m":
					apiRef.current.toggleMute();
					break;
				case "f":
					apiRef.current.toggleFullscreen();
					break;
				case "c":
					apiRef.current.toggleSubtitles();
					break;
				case "Escape": if (!document.fullscreenElement) onBack();
			}
			showControls();
		};
		window.addEventListener("keydown", onKey);
		return () => window.removeEventListener("keydown", onKey);
	}, [onBack, showControls]);
	useEffect(() => {
		const onFs = () => setFullscreen(Boolean(document.fullscreenElement));
		document.addEventListener("fullscreenchange", onFs);
		return () => document.removeEventListener("fullscreenchange", onFs);
	}, []);
	useEffect(() => {
		showControls();
		return () => {
			if (hideTimer.current) window.clearTimeout(hideTimer.current);
		};
	}, [showControls]);
	if (source.kind === "iframe") {
		let iframeUrl = source.url;
		iframeUrl = iframeUrl.replace("{id}", source.mediaId);
		iframeUrl = iframeUrl.replace("{season}", String(source.seasonNumber ?? 1));
		iframeUrl = iframeUrl.replace("{episode}", String(source.episodeNumber ?? 1));
		return /* @__PURE__ */ jsxs("div", {
			ref: shellRef,
			className: "relative size-full bg-black",
			children: [/* @__PURE__ */ jsx("iframe", {
				src: iframeUrl,
				title,
				allow: "autoplay; fullscreen; encrypted-media",
				allowFullScreen: true,
				className: "size-full border-0"
			}), /* @__PURE__ */ jsxs(Button, {
				variant: "secondary",
				className: "absolute left-4 top-4 rounded-full",
				onClick: onBack,
				"data-focusable": true,
				children: [/* @__PURE__ */ jsx(ArrowLeft, { className: "size-4" }), " Back"]
			})]
		});
	}
	const VolumeIcon = muted || volume === 0 ? VolumeX : volume < .5 ? Volume1 : Volume2;
	return /* @__PURE__ */ jsxs("div", {
		ref: shellRef,
		className: "relative size-full select-none bg-black",
		onMouseMove: showControls,
		onClick: showControls,
		children: [
			/* @__PURE__ */ jsx("video", {
				ref: videoRef,
				poster: poster ?? void 0,
				playsInline: true,
				autoPlay: true,
				className: "size-full object-contain",
				onLoadedMetadata: (e) => {
					const v = e.currentTarget;
					setDuration(v.duration || 0);
					if (startAt > 0 && startAt < (v.duration || Infinity) - 10) v.currentTime = startAt;
					setReady(true);
				},
				onTimeUpdate: (e) => {
					const v = e.currentTarget;
					setPosition(v.currentTime);
					onProgress?.(v.currentTime, v.duration || 0);
				},
				onPlay: () => setPaused(false),
				onPause: () => setPaused(true),
				onWaiting: () => setBuffering(true),
				onPlaying: () => setBuffering(false),
				onEnded: () => onEnded?.(),
				onError: () => setError("This source could not be played."),
				onDoubleClick: () => apiRef.current.toggleFullscreen(),
				children: source.subtitles.map((track, i) => /* @__PURE__ */ jsx("track", {
					kind: "subtitles",
					src: track.url,
					srcLang: track.lang,
					label: track.label,
					default: subsOn && i === 0
				}, track.url))
			}),
			(!ready || buffering) && !error ? /* @__PURE__ */ jsx("div", {
				className: "pointer-events-none absolute inset-0 grid place-items-center",
				children: /* @__PURE__ */ jsx(Loader2, { className: "size-12 animate-spin text-primary" })
			}) : null,
			error ? /* @__PURE__ */ jsx("div", {
				className: "absolute inset-0 grid place-items-center bg-background/90 px-6 text-center",
				children: /* @__PURE__ */ jsxs("div", {
					className: "max-w-sm space-y-4",
					children: [
						/* @__PURE__ */ jsx("h2", {
							className: "text-xl font-semibold",
							children: "Playback problem"
						}),
						/* @__PURE__ */ jsx("p", {
							className: "text-sm text-muted-foreground",
							children: error
						}),
						/* @__PURE__ */ jsx(Button, {
							onClick: onBack,
							"data-focusable": true,
							children: "Go back"
						})
					]
				})
			}) : null,
			/* @__PURE__ */ jsxs("div", {
				className: cn("pointer-events-none absolute inset-0 flex flex-col justify-between bg-gradient-to-b from-black/70 via-transparent to-black/90 transition-opacity duration-300", controlsVisible ? "opacity-100" : "opacity-0"),
				children: [/* @__PURE__ */ jsxs("div", {
					className: "pointer-events-auto flex items-start gap-4 p-4 md:p-6",
					children: [/* @__PURE__ */ jsx(Button, {
						variant: "ghost",
						size: "icon",
						onClick: onBack,
						"data-focusable": true,
						"aria-label": "Back",
						children: /* @__PURE__ */ jsx(ArrowLeft, { className: "size-6" })
					}), /* @__PURE__ */ jsxs("div", {
						className: "min-w-0",
						children: [/* @__PURE__ */ jsx("p", {
							className: "truncate text-lg font-semibold md:text-2xl",
							children: title
						}), subtitle ? /* @__PURE__ */ jsx("p", {
							className: "truncate text-sm text-muted-foreground",
							children: subtitle
						}) : null]
					})]
				}), /* @__PURE__ */ jsxs("div", {
					className: "pointer-events-auto space-y-3 p-4 md:p-8",
					children: [/* @__PURE__ */ jsx(Slider, {
						value: [duration ? position / duration * 100 : 0],
						onValueChange: ([v]) => apiRef.current.seekTo((v ?? 0) / 100 * duration),
						max: 100,
						step: .1,
						"aria-label": "Seek"
					}), /* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-2 text-sm",
						children: [
							/* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								onClick: () => apiRef.current.playPause(),
								"data-focusable": true,
								"aria-label": paused ? "Play" : "Pause",
								children: paused ? /* @__PURE__ */ jsx(Play, { className: "size-6 fill-current" }) : /* @__PURE__ */ jsx(Pause, { className: "size-6" })
							}),
							/* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								onClick: () => apiRef.current.seekBy(-10),
								"data-focusable": true,
								"aria-label": "Back 10 seconds",
								children: /* @__PURE__ */ jsx(RotateCcw, { className: "size-5" })
							}),
							/* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								onClick: () => apiRef.current.seekBy(10),
								"data-focusable": true,
								"aria-label": "Forward 10 seconds",
								children: /* @__PURE__ */ jsx(RotateCw, { className: "size-5" })
							}),
							onPrev ? /* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								onClick: onPrev,
								"data-focusable": true,
								"aria-label": "Previous episode",
								children: /* @__PURE__ */ jsx(SkipBack, { className: "size-5" })
							}) : null,
							onNext ? /* @__PURE__ */ jsx(Button, {
								size: "icon",
								variant: "ghost",
								onClick: onNext,
								"data-focusable": true,
								"aria-label": "Next episode",
								children: /* @__PURE__ */ jsx(SkipForward, { className: "size-5" })
							}) : null,
							/* @__PURE__ */ jsxs("div", {
								className: "ml-1 hidden items-center gap-2 sm:flex",
								children: [/* @__PURE__ */ jsx(Button, {
									size: "icon",
									variant: "ghost",
									onClick: () => apiRef.current.toggleMute(),
									"data-focusable": true,
									"aria-label": "Mute",
									children: /* @__PURE__ */ jsx(VolumeIcon, { className: "size-5" })
								}), /* @__PURE__ */ jsx(Slider, {
									className: "w-24",
									value: [muted ? 0 : volume * 100],
									onValueChange: ([v]) => apiRef.current.setVolume((v ?? 0) / 100),
									max: 100,
									"aria-label": "Volume"
								})]
							}),
							/* @__PURE__ */ jsxs("span", {
								className: "ml-2 tabular-nums text-muted-foreground",
								children: [
									formatTime(position),
									" / ",
									formatTime(duration)
								]
							}),
							/* @__PURE__ */ jsxs("div", {
								className: "ml-auto flex items-center gap-1",
								children: [source.subtitles.length ? /* @__PURE__ */ jsx(Button, {
									size: "icon",
									variant: subsOn ? "default" : "ghost",
									onClick: () => apiRef.current.toggleSubtitles(),
									"data-focusable": true,
									"aria-label": "Subtitles",
									children: /* @__PURE__ */ jsx(Subtitles, { className: "size-5" })
								}) : null, /* @__PURE__ */ jsx(Button, {
									size: "icon",
									variant: "ghost",
									onClick: () => apiRef.current.toggleFullscreen(),
									"data-focusable": true,
									"aria-label": "Fullscreen",
									children: fullscreen ? /* @__PURE__ */ jsx(Minimize, { className: "size-5" }) : /* @__PURE__ */ jsx(Maximize, { className: "size-5" })
								})]
							})
						]
					})]
				})]
			})
		]
	});
}
//#endregion
//#region src/routes/watch.$type.$id.tsx?tsr-split=component
function WatchPage() {
	const { type, id } = Route.useParams();
	const { season, episode, t } = Route.useSearch();
	const router = useRouter();
	const navigate = useNavigate({ from: "/watch/$type/$id" });
	const mediaType = type === "tv" ? "tv" : "movie";
	const numericId = Number(id);
	const [selectedSourceIndex, setSelectedSourceIndex] = useState(0);
	const profile = useProfile();
	const movie = useQuery({
		...movieQuery(numericId),
		enabled: mediaType === "movie"
	});
	const show = useQuery({
		...showQuery(numericId),
		enabled: mediaType === "tv"
	});
	const episodes = useQuery({
		...seasonQuery(numericId, season ?? 1),
		enabled: mediaType === "tv"
	});
	const sources = useQuery(sourcesQuery({
		mediaType,
		metadataId: String(numericId),
		seasonNumber: mediaType === "tv" ? season ?? 1 : null,
		episodeNumber: mediaType === "tv" ? episode ?? 1 : null
	}));
	useEffect(() => {
		console.log("[WatchPage] Sources query result:", {
			mediaType,
			numericId,
			isLoading: sources.isLoading,
			isError: sources.isError,
			error: sources.error,
			dataLength: sources.data?.length,
			data: sources.data?.map((s) => ({
				name: s.name,
				kind: s.kind,
				url: s.url
			}))
		});
	}, [
		sources.data,
		sources.isLoading,
		sources.isError,
		sources.error,
		mediaType,
		numericId
	]);
	const detail = mediaType === "tv" ? show.data : movie.data;
	const currentEpisode = episodes.data?.find((e) => e.episodeNumber === (episode ?? 1));
	const preferredSourceName = profile.preferences.preferredSourceName;
	const source = preferredSourceName && sources.data ? sources.data.find((s) => s.name === preferredSourceName) || sources.data[0] : sources.data?.[selectedSourceIndex];
	const saved = detail ? getPosition(mediaType, numericId, season ?? null, episode ?? null) : void 0;
	const goBack = () => {
		if (window.history.length > 1) router.history.back();
		else navigate({
			to: mediaType === "tv" ? "/show/$id" : "/movie/$id",
			params: { id: String(numericId) }
		});
	};
	if (sources.isLoading || !detail && (movie.isLoading || show.isLoading)) return /* @__PURE__ */ jsx("div", {
		className: "grid min-h-screen place-items-center bg-black text-muted-foreground",
		children: "Loading…"
	});
	if (!source || !detail) return /* @__PURE__ */ jsx("div", {
		className: "grid min-h-screen place-items-center bg-background px-6",
		children: /* @__PURE__ */ jsx(EmptyState, {
			title: "No playable source",
			description: "This title has no source configured yet. Add one from the library manager and try again."
		})
	});
	const title = detail.title;
	const subtitle = mediaType === "tv" && currentEpisode ? `S${season ?? 1} E${currentEpisode.episodeNumber} • ${currentEpisode.title}` : null;
	const goToEpisode = (delta) => {
		if (mediaType !== "tv") return;
		const next = (episode ?? 1) + delta;
		if (next < 1) return;
		navigate({ search: (prev) => ({
			...prev,
			episode: next,
			t: void 0
		}) });
	};
	return /* @__PURE__ */ jsx("div", {
		className: "h-screen w-screen bg-black",
		children: /* @__PURE__ */ jsx(VideoPlayer, {
			source,
			title,
			subtitle,
			poster: detail.backdrop,
			startAt: t ?? saved?.position ?? 0,
			onBack: goBack,
			onProgress: (position, duration) => saveProgress({
				mediaType,
				mediaId: numericId,
				seasonNumber: season ?? null,
				episodeNumber: episode ?? null,
				position,
				duration,
				title,
				poster: detail.poster,
				backdrop: detail.backdrop,
				episodeTitle: subtitle
			}),
			onEnded: () => goToEpisode(1),
			...mediaType === "tv" ? {
				onNext: () => goToEpisode(1),
				onPrev: () => goToEpisode(-1)
			} : {}
		})
	});
}
//#endregion
export { WatchPage as component };
